# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Node
node_modules/
dist/
.vite/

# env / segredos (NUNCA commitar — este repositorio e publico)
.env
.env.*
!.env.example
*.key
*.pem
*.p12

# ferramentas locais do agente (carregam contexto interno — nao versionar)
.claude/

# OS / editores
.DS_Store
.idea/
.vscode/
*.log

# ...mas os logs de IA sao entregavel — ver ai-logs/README.md
!ai-logs/**

# Downloads e capturas do Playwright MCP. São subprodutos de sessão, não artefato:
# entraram no repositório uma vez e trouxeram junto coordenadas SVG com oito casas
# decimais, que a varredura de PII acusa como CEP — com razão, porque ela não tem
# como saber que oito dígitos ali são precisão de ponto flutuante.
.playwright-mcp/
